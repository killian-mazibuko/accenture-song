{
  "name": "accenture-song",
  "version": "1.0.0",
  "description": "A Full-Stack Web Application based on a Figma mock from the Accenture Team",
  "main": "index.js",
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "node backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
